# health-report-analyzer
HealthAI Analyzer is an intelligent system that processes structured blood report data to extract clinical parameters. It validates values, standardizes units, and compares results with medical reference ranges. Each parameter is classified as Normal, High, or Low using rule-based logic for accurate analysis.
